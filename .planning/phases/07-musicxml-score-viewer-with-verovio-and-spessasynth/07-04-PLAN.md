---
phase: 07-musicxml-score-viewer-with-verovio-and-spessasynth
plan: 04
type: execute
wave: 4
depends_on: [07-03]
files_modified:
  - app/components/MidiTrackPlayer.tsx
  - public/audio/salamander/
autonomous: false
requirements: [D-06]

must_haves:
  truths:
    - "MidiTrackPlayer.tsx is deleted from the codebase"
    - "No imports of tone or @tonejs/midi remain anywhere"
    - "Salamander piano samples are removed from public/"
    - "Score viewer renders MusicXML and plays with cursor sync"
    - "MidiPlayer works as drop-in replacement in HymnDetailModal"
  artifacts: []
  key_links: []
---

<objective>
Remove the old MidiTrackPlayer component, Tone.js Salamander samples, and verify the complete feature works end-to-end with human testing.

Purpose: Clean up legacy code and get human confirmation that the score viewer and MIDI migration work correctly.
Output: Clean codebase with no Tone.js remnants, human-verified feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-musicxml-score-viewer-with-verovio-and-spessasynth/07-CONTEXT.md
@.planning/phases/07-musicxml-score-viewer-with-verovio-and-spessasynth/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete MidiTrackPlayer, Salamander samples, and verify no Tone.js remnants</name>
  <files>app/components/MidiTrackPlayer.tsx, public/audio/salamander/</files>
  <read_first>
    - app/components/MidiTrackPlayer.tsx (confirm it exists and is no longer imported anywhere)
    - app/empaquetador/components/HymnDetailModal.tsx (confirm it imports MidiPlayer, NOT MidiTrackPlayer)
  </read_first>
  <action>
1. Verify no file in the codebase imports MidiTrackPlayer:
   ```bash
   grep -r "MidiTrackPlayer" app/ --include="*.tsx" --include="*.ts" -l
   ```
   This should return ONLY `app/components/MidiTrackPlayer.tsx` itself. If any other file still imports it, fix the import first.

2. Delete `app/components/MidiTrackPlayer.tsx`

3. Delete `public/audio/salamander/` directory (Tone.js piano samples — no longer needed with SpessaSynth SoundFont approach)

4. Verify no imports of `tone` or `@tonejs/midi` remain:
   ```bash
   grep -r "from 'tone'" app/ --include="*.tsx" --include="*.ts" -l
   grep -r "from '@tonejs/midi'" app/ --include="*.tsx" --include="*.ts" -l
   ```
   Both should return empty. If any remain, update those imports.

5. Verify package.json no longer lists tone or @tonejs/midi (should already be removed in Plan 01).

6. Run `npm run build` to confirm clean build with no broken imports.
  </action>
  <verify>
    <automated>! test -f app/components/MidiTrackPlayer.tsx && echo "MidiTrackPlayer deleted OK"; ! test -d public/audio/salamander && echo "Salamander deleted OK"; ! grep -r "MidiTrackPlayer" app/ --include="*.tsx" --include="*.ts" -l 2>/dev/null | grep -v "^$" && echo "No MidiTrackPlayer imports OK"; ! grep -r "from 'tone'" app/ --include="*.tsx" --include="*.ts" 2>/dev/null | grep -v "^$" && echo "No tone imports OK"; ! grep -q '"tone"' package.json && echo "No tone in package.json OK"</automated>
  </verify>
  <acceptance_criteria>
    - app/components/MidiTrackPlayer.tsx does NOT exist
    - public/audio/salamander/ directory does NOT exist
    - No file in app/ contains `import` of `MidiTrackPlayer`
    - No file in app/ contains `from 'tone'` or `from '@tonejs/midi'`
    - package.json does NOT contain "tone" or "@tonejs/midi"
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>All Tone.js code and assets removed, build succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of score viewer and MIDI migration</name>
  <files>none</files>
  <action>Present the verification checklist to the user and wait for approval. No code changes in this task.</action>
  <verify>User types "approved"</verify>
  <done>User has verified all 4 test scenarios pass</done>
  <what-built>
    Complete MusicXML Score Viewer with Verovio rendering and SpessaSynth playback:
    1. Score rendering: Verovio renders MusicXML as SVG in the Partituras tab
    2. MIDI playback: SpessaSynth plays Verovio-generated MIDI with SoundFont
    3. Cursor sync: Notes highlight in real-time during playback
    4. Auto-scroll: Page changes automatically when playback crosses page boundaries
    5. Zoom controls: Scale the score up/down
    6. Seek bar: Scrub through playback
    7. MidiPlayer migration: All MIDI playback in HymnDetailModal uses SpessaSynth (no more Tone.js)
    8. Keyboard shortcuts: Space (play/pause), arrows (seek), +/- (zoom)
  </what-built>
  <how-to-verify>
    **Prerequisites:**
    - Set `SOUNDFONT_FILE_ID` environment variable to a valid SoundFont file UUID in Directus
    - Start dev server: `npm run dev`

    **Test 1: Score Viewer (hymn WITH MusicXML)**
    1. Navigate to the empaquetador or HymnExplorer
    2. Click the eye icon on a hymn that has a MusicXML file in Directus materials
    3. Click the "Partitura" tab
    4. Expected: SVG score renders showing SATB notation
    5. Click Play button — audio should start, notes should highlight as they play
    6. Verify cursor advances note-by-note
    7. If score has multiple pages, verify auto-scroll when playback crosses page boundary
    8. Use zoom +/- buttons — score should rescale
    9. Drag seek bar — playback should jump to new position
    10. Press Space key — should toggle play/pause

    **Test 2: No MusicXML fallback**
    1. Click eye icon on a hymn WITHOUT MusicXML
    2. Click "Partitura" tab
    3. Expected: Shows "Partitura" heading with "Proximamente" text (unchanged placeholder)

    **Test 3: MIDI Player migration**
    1. Click eye icon on a hymn with MIDI file
    2. Look at audio tracks section (right column on desktop)
    3. Click play on the MIDI track
    4. Expected: MIDI plays with SoundFont (should sound like real instruments, not piano-only)
    5. Progress bar advances, time display updates
    6. Download button works

    **Test 4: SoundFont caching**
    1. After first play, reload the page
    2. Play a score again
    3. Expected: SoundFont loads instantly from cache (no "Descargando sonidos..." progress)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- MidiTrackPlayer.tsx deleted
- Salamander samples deleted
- No Tone.js remnants in codebase
- Human confirms score viewer works with cursor sync
- Human confirms MIDI playback migration works
- Human confirms SoundFont caching works
</verification>

<success_criteria>
- Zero references to Tone.js, @tonejs/midi, MidiTrackPlayer, or Salamander samples
- Human approves score viewer rendering, playback, cursor sync, and zoom
- Human approves MIDI player migration in HymnDetailModal
- Clean build passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-musicxml-score-viewer-with-verovio-and-spessasynth/07-04-SUMMARY.md`
</output>
