---
phase: 05-impresion-himnos
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - app/empaquetador/components/StepConfiguracion.tsx
autonomous: false
requirements:
  - PRINT-01
  - PRINT-03
  - PRINT-04
  - PRINT-05
  - PRINT-06

must_haves:
  truths:
    - "User sees print mode selector (Hoja Simple / Booklet) as first control in left column"
    - "Selecting Booklet hides Diseno de Pagina and Orientacion sections"
    - "User sees orientation selector (Vertical / Horizontal) when in simple mode"
    - "User sees font preset selector (Clasica / Moderna / Legible) with descriptions"
    - "User sees bible reference toggle switch defaulting to on"
    - "Booklet warning appears when printMode is booklet and selectedHymns count exceeds 40"
    - "Booklet instruction text shows when booklet mode is selected"
  artifacts:
    - path: "app/empaquetador/components/StepConfiguracion.tsx"
      provides: "Updated Step 2 with all new controls"
      contains: "SET_PRINT_MODE"
  key_links:
    - from: "app/empaquetador/components/StepConfiguracion.tsx"
      to: "app/empaquetador/hooks/useWizardReducer.ts"
      via: "dispatches new actions (SET_PRINT_MODE, SET_ORIENTATION, SET_FONT_PRESET, SET_INCLUDE_BIBLE_REF)"
      pattern: "dispatch.*SET_PRINT_MODE"
---

<objective>
Update the wizard Step 2 (StepConfiguracion) with all new print configuration controls: print mode selector, orientation selector, font preset selector, bible reference toggle, booklet warning, and conditional visibility logic.

Purpose: This is the user-facing UI that exposes all the new printing options. It wires the existing wizard state (Plan 01) to visual controls so users can configure their print output.
Output: Updated StepConfiguracion.tsx with all new controls matching the UI-SPEC exactly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-implementa-feature-completo-de-impresi-n-de-himnos-modo-hoja-simple-modo-booklet-con-imposici-n-saddle-stitch-opciones-de-orientaci-n-fuente-formato-vers-culo/05-CONTEXT.md
@.planning/phases/05-implementa-feature-completo-de-impresi-n-de-himnos-modo-hoja-simple-modo-booklet-con-imposici-n-saddle-stitch-opciones-de-orientaci-n-fuente-formato-vers-culo/05-UI-SPEC.md
@.planning/phases/05-implementa-feature-completo-de-impresi-n-de-himnos-modo-hoja-simple-modo-booklet-con-imposici-n-saddle-stitch-opciones-de-orientaci-n-fuente-formato-vers-culo/05-01-SUMMARY.md
@.planning/phases/05-implementa-feature-completo-de-impresi-n-de-himnos-modo-hoja-simple-modo-booklet-con-imposici-n-saddle-stitch-opciones-de-orientaci-n-fuente-formato-vers-culo/05-02-SUMMARY.md

<interfaces>
<!-- Interfaces from Plan 01 that this plan consumes -->

From app/empaquetador/hooks/useWizardReducer.ts (extended in Plan 01):
```typescript
export interface WizardState {
  step: 1 | 2 | 3;
  selectedHymns: HymnSearchResult[];
  layout: 'one-per-page' | 'two-per-page';
  style: 'decorated' | 'plain';
  printMode: 'simple' | 'booklet';
  orientation: 'portrait' | 'landscape';
  fontPreset: 'clasica' | 'moderna' | 'legible';
  includeBibleRef: boolean;
  audioSelections: Map<string, Set<string>>;
  isGenerating: boolean;
  error: string | null;
}

export type WizardAction =
  | { type: 'SET_PRINT_MODE'; printMode: 'simple' | 'booklet' }
  | { type: 'SET_ORIENTATION'; orientation: 'portrait' | 'landscape' }
  | { type: 'SET_FONT_PRESET'; fontPreset: 'clasica' | 'moderna' | 'legible' }
  | { type: 'SET_INCLUDE_BIBLE_REF'; includeBibleRef: boolean }
  | ... (existing actions);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update StepConfiguracion with all new print controls</name>
  <files>app/empaquetador/components/StepConfiguracion.tsx</files>
  <read_first>
    app/empaquetador/components/StepConfiguracion.tsx,
    app/empaquetador/hooks/useWizardReducer.ts,
    .planning/phases/05-implementa-feature-completo-de-impresi-n-de-himnos-modo-hoja-simple-modo-booklet-con-imposici-n-saddle-stitch-opciones-de-orientaci-n-fuente-formato-vers-culo/05-UI-SPEC.md
  </read_first>
  <action>
    Update `StepConfiguracion.tsx` to add all new controls in the left column. The right column (audio selection) is UNCHANGED.

    **New imports to add:**
    ```typescript
    import { Switch } from '@/lib/shadcn/ui';
    import { FileText, Grid2X2, Sparkles, LayoutList, Music, BookOpen, Smartphone, Type, ALargeSmall, ZoomIn, AlertTriangle } from 'lucide-react';
    ```
    Note: `FileText`, `Grid2X2`, `Sparkles`, `LayoutList`, `Music` are already imported. Add: `BookOpen`, `Smartphone`, `Type`, `ALargeSmall`, `ZoomIn`, `AlertTriangle`. Also add `Switch` to the shadcn import.

    **Left column `<CardContent className="space-y-6">` new order (top to bottom):**

    **1. MODO DE IMPRESION (NEW, first section)** -- per D-03:
    ```tsx
    <div>
      <Label className="text-xs font-semibold tracking-wide uppercase text-slate-400 mb-3 block">
        Modo de Impresion
      </Label>
      <div className="grid grid-cols-2 gap-3">
        <button type="button"
          onClick={() => dispatch({ type: 'SET_PRINT_MODE', printMode: 'simple' })}
          className={cn(
            'flex flex-col items-center gap-2 p-4 rounded-lg border-2 transition-all min-h-[48px]',
            state.printMode === 'simple'
              ? 'border-primary bg-primary/5 text-primary'
              : 'border-slate-200 hover:border-slate-300 text-slate-600',
          )}>
          <FileText className="h-8 w-8" />
          <span className="text-sm font-medium">Hoja Simple</span>
        </button>
        <button type="button"
          onClick={() => dispatch({ type: 'SET_PRINT_MODE', printMode: 'booklet' })}
          className={cn(
            'flex flex-col items-center gap-2 p-4 rounded-lg border-2 transition-all min-h-[48px]',
            state.printMode === 'booklet'
              ? 'border-primary bg-primary/5 text-primary'
              : 'border-slate-200 hover:border-slate-300 text-slate-600',
          )}>
          <BookOpen className="h-8 w-8" />
          <span className="text-sm font-medium">Booklet</span>
        </button>
      </div>
      {state.printMode === 'booklet' && (
        <p className="text-xs text-slate-500 mt-2">
          Para imprimir: seleccione &quot;Ambos lados&quot; y &quot;Voltear en borde corto&quot;. Engrapadora al centro.
        </p>
      )}
    </div>
    ```

    **2. `<Separator />`**

    **3. DISENO DE PAGINA (EXISTING, conditionally visible)** -- per D-03:
    Wrap the existing "Diseno de Pagina" section in:
    ```tsx
    {state.printMode === 'simple' && (
      <>
        <div>
          {/* existing layout selector code unchanged */}
        </div>
        <Separator />
      </>
    )}
    ```

    **4. ORIENTACION (NEW, conditionally visible)** -- per D-04:
    Only visible when `state.printMode === 'simple'`:
    ```tsx
    {state.printMode === 'simple' && (
      <>
        <div>
          <Label className="text-xs font-semibold tracking-wide uppercase text-slate-400 mb-3 block">
            Orientacion
          </Label>
          <div className="grid grid-cols-2 gap-3">
            <button type="button"
              onClick={() => dispatch({ type: 'SET_ORIENTATION', orientation: 'portrait' })}
              className={cn(
                'flex flex-col items-center gap-2 p-4 rounded-lg border-2 transition-all min-h-[48px]',
                state.orientation === 'portrait'
                  ? 'border-primary bg-primary/5 text-primary'
                  : 'border-slate-200 hover:border-slate-300 text-slate-600',
              )}>
              <Smartphone className="h-8 w-8" />
              <span className="text-sm font-medium">Vertical</span>
            </button>
            <button type="button"
              onClick={() => dispatch({ type: 'SET_ORIENTATION', orientation: 'landscape' })}
              className={cn(
                'flex flex-col items-center gap-2 p-4 rounded-lg border-2 transition-all min-h-[48px]',
                state.orientation === 'landscape'
                  ? 'border-primary bg-primary/5 text-primary'
                  : 'border-slate-200 hover:border-slate-300 text-slate-600',
              )}>
              <Smartphone className="h-8 w-8 rotate-90" />
              <span className="text-sm font-medium">Horizontal</span>
            </button>
          </div>
        </div>
        <Separator />
      </>
    )}
    ```

    **5. ESTILO VISUAL (EXISTING, always visible)** -- unchanged content, just position after orientation.

    **6. `<Separator />`**

    **7. FUENTE (NEW, always visible)** -- per D-06:
    List-style selector with icon-left layout:
    ```tsx
    <div>
      <Label className="text-xs font-semibold tracking-wide uppercase text-slate-400 mb-3 block">
        Fuente
      </Label>
      <div className="space-y-3">
        {([
          { value: 'clasica' as const, label: 'Clasica', desc: 'Tipografia serif elegante (Adamina)', icon: Type },
          { value: 'moderna' as const, label: 'Moderna', desc: 'Tipografia sans-serif limpia (Helvetica)', icon: ALargeSmall },
          { value: 'legible' as const, label: 'Legible', desc: 'Tamano grande para lectura facil', icon: ZoomIn },
        ]).map(({ value, label, desc, icon: Icon }) => (
          <button key={value} type="button"
            onClick={() => dispatch({ type: 'SET_FONT_PRESET', fontPreset: value })}
            className={cn(
              'w-full text-left p-4 rounded-lg border-2 transition-all min-h-[48px]',
              state.fontPreset === value
                ? 'border-primary bg-primary/5'
                : 'border-slate-200 hover:border-slate-300',
            )}>
            <div className="flex items-center gap-2 mb-1">
              <Icon className={cn('h-4 w-4', state.fontPreset === value ? 'text-primary' : 'text-slate-500')} />
              <span className={cn('text-sm font-semibold', state.fontPreset === value ? 'text-primary' : 'text-slate-700')}>
                {label}
              </span>
            </div>
            <p className="text-xs text-slate-500 pl-6">{desc}</p>
          </button>
        ))}
      </div>
    </div>
    ```

    **8. `<Separator />`**

    **9. REFERENCIA BIBLICA (NEW, always visible)** -- per D-09:
    ```tsx
    <div className="flex items-center gap-3">
      <Switch
        id="include-bible-ref"
        checked={state.includeBibleRef}
        onCheckedChange={(checked) =>
          dispatch({ type: 'SET_INCLUDE_BIBLE_REF', includeBibleRef: Boolean(checked) })
        }
      />
      <div>
        <Label htmlFor="include-bible-ref" className="text-sm cursor-pointer">
          Incluir referencia biblica
        </Label>
        <p className="text-xs text-slate-500">Muestra el texto y cita biblica en el PDF</p>
      </div>
    </div>
    ```

    **10. BOOKLET WARNING (NEW, conditional)** -- per UI-SPEC:
    ```tsx
    {state.printMode === 'booklet' && state.selectedHymns.length > 40 && (
      <div className="rounded-lg border border-amber-200 bg-amber-50 p-4 flex items-start gap-2">
        <AlertTriangle className="h-4 w-4 text-amber-500 flex-shrink-0 mt-0.5" />
        <p className="text-xs text-amber-600">
          Los booklets con mas de 40 paginas son dificiles de engrapar. Considere dividir en varios paquetes.
        </p>
      </div>
    )}
    ```

    **Additional changes:**
    - Update `min-h-[44px]` to `min-h-[48px]` on ALL existing selector buttons (layout and style buttons) per UI-SPEC touch target requirement.
    - Update CardTitle text from `font-bold` to `font-semibold` if present per UI-SPEC typography.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `SET_PRINT_MODE`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `SET_ORIENTATION`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `SET_FONT_PRESET`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `SET_INCLUDE_BIBLE_REF`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `Modo de Impresion`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `Hoja Simple`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `Booklet`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `Orientacion`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `Vertical`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `Horizontal`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `Fuente`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `Clasica`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `Moderna`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `Legible`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `Incluir referencia biblica`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `Switch`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `AlertTriangle`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `Voltear en borde corto`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `min-h-[48px]`
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `state.printMode === 'simple'` (conditional visibility)
    - `app/empaquetador/components/StepConfiguracion.tsx` contains `state.printMode === 'booklet'` (warning condition)
  </acceptance_criteria>
  <done>
    StepConfiguracion shows print mode, orientation, font preset, bible ref toggle, and booklet warning. Conditional visibility hides layout/orientation when booklet is selected. All text matches UI-SPEC copy exactly. All touch targets are 48px minimum.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify printing feature UI and PDF output</name>
  <files>app/empaquetador/components/StepConfiguracion.tsx</files>
  <action>Human verification of the complete printing feature UI and generated PDF output. No code changes -- visual and functional check only.</action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>User approves the UI layout and PDF output for all new options (print mode, orientation, font presets, bible ref toggle, booklet mode).</done>
  <what-built>Complete printing feature: print mode (Hoja Simple/Booklet), orientation, font presets (Clasica/Moderna/Legible), bible reference toggle, booklet warning, and all PDF generation with saddle-stitch imposition.</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Navigate to http://localhost:3000/empaquetador
    3. Select 2-3 hymns in Step 1
    4. Go to Step 2 and verify:
       a. "Modo de Impresion" selector appears FIRST with "Hoja Simple" selected by default
       b. Click "Booklet" -- "Diseno de Pagina" and "Orientacion" sections disappear; booklet duplex instruction text shows
       c. Click "Hoja Simple" -- sections reappear
       d. "Orientacion" shows Vertical/Horizontal with phone icon (rotated for horizontal)
       e. "Fuente" shows Clasica/Moderna/Legible with descriptions
       f. "Incluir referencia biblica" switch is ON by default, toggleable
    5. Generate a package in Hoja Simple mode -- verify PDF downloads and opens correctly
    6. Generate a package in Booklet mode -- verify PDF has landscape pages (check visually)
    7. Switch font to "Legible" and generate -- verify PDF text appears larger
    8. Toggle bible reference OFF and generate -- verify PDF omits bible text section
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- full test suite passes
- `npm run build` -- production build succeeds
- Visual verification of all new controls in Step 2
- PDF output verification for booklet mode, landscape, font presets, bible ref toggle
</verification>

<success_criteria>
- All 6 new control groups visible in Step 2 per UI-SPEC layout order
- Conditional visibility works (booklet hides layout + orientation)
- Booklet warning appears for >40 hymns
- Booklet duplex instruction text shows for booklet mode
- All selector buttons have 48px minimum touch target
- All text in Spanish per copywriting contract
- Generated PDFs reflect the selected options
</success_criteria>

<output>
After completion, create `.planning/phases/05-implementa-feature-completo-de-impresi-n-de-himnos-modo-hoja-simple-modo-booklet-con-imposici-n-saddle-stitch-opciones-de-orientaci-n-fuente-formato-vers-culo/05-03-SUMMARY.md`
</output>
