---
phase: 04-wizard-ui-and-download-experience
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/empaquetador/page.tsx
  - app/empaquetador/components/WizardStepper.tsx
  - app/empaquetador/components/StepSeleccion.tsx
  - app/empaquetador/components/HymnResultRow.tsx
  - app/empaquetador/components/SelectedHymnChip.tsx
autonomous: true
requirements:
  - BUSQ-01
  - BUSQ-02
  - BUSQ-03
  - BUSQ-04
  - BUSQ-05
  - BUSQ-06
  - BUSQ-07
  - UX-01
  - UX-02
  - UX-03

must_haves:
  truths:
    - "User can type in search box and see hymn results appear after debounce"
    - "User can filter results by hymnal dropdown and category dropdown"
    - "User can click Agregar to add a hymn to selection"
    - "User can see selected hymns in sidebar (desktop) or drawer (mobile)"
    - "User can click X to remove a hymn from selection"
    - "Already-selected hymns show Seleccionado state in results"
    - "Wizard stepper shows 3 steps with active step highlighted"
    - "Siguiente button is disabled when no hymns are selected"
  artifacts:
    - path: "app/empaquetador/page.tsx"
      provides: "Top-level wizard page with useReducer state and step rendering"
      contains: "'use client'"
    - path: "app/empaquetador/components/WizardStepper.tsx"
      provides: "3-step horizontal stepper with active/completed/upcoming states"
      contains: "Seleccionar Himnos"
    - path: "app/empaquetador/components/StepSeleccion.tsx"
      provides: "Step 1: search input, filter dropdowns, results list, selected panel"
      contains: "Buscar por numero o nombre"
    - path: "app/empaquetador/components/HymnResultRow.tsx"
      provides: "Single search result row with Agregar button"
      contains: "Agregar"
    - path: "app/empaquetador/components/SelectedHymnChip.tsx"
      provides: "Selected hymn display with remove X button"
      contains: "Quitar"
  key_links:
    - from: "app/empaquetador/page.tsx"
      to: "app/empaquetador/hooks/useWizardReducer.ts"
      via: "useWizardReducer() hook call"
      pattern: "useWizardReducer"
    - from: "app/empaquetador/components/StepSeleccion.tsx"
      to: "app/empaquetador/hooks/useHymnSearch.ts"
      via: "useHymnSearch() hook call"
      pattern: "useHymnSearch"
    - from: "app/empaquetador/components/StepSeleccion.tsx"
      to: "/api/hymnals and /api/categories"
      via: "fetch on mount for filter dropdowns"
      pattern: "fetch.*api/hymnals"
---

<objective>
Build the wizard shell page and the complete Step 1 (Seleccionar Himnos) UI: the WizardStepper component, search input with filters, results list, selected hymns sidebar/drawer, and navigation buttons.

Purpose: This is the primary user interaction surface — search, browse, and select hymns. It consumes all the hooks and APIs from Plan 01.
Output: Functional /empaquetador page with Step 1 fully interactive (search, filter, select, remove hymns). Steps 2 and 3 render placeholder content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wizard-ui-and-download-experience/04-CONTEXT.md
@.planning/phases/04-wizard-ui-and-download-experience/04-UI-SPEC.md
@.planning/phases/04-wizard-ui-and-download-experience/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor should use these directly -->

From app/empaquetador/hooks/useWizardReducer.ts:
```typescript
export interface WizardState {
  step: 1 | 2 | 3;
  selectedHymns: HymnSearchResult[];
  layout: 'one-per-page' | 'two-per-page';
  style: 'decorated' | 'plain';
  audioSelections: Map<string, Set<string>>;
  isGenerating: boolean;
  error: string | null;
}

export type WizardAction =
  | { type: 'SET_STEP'; step: 1 | 2 | 3 }
  | { type: 'ADD_HYMN'; hymn: HymnSearchResult }
  | { type: 'REMOVE_HYMN'; hymnId: string }
  | { type: 'SET_LAYOUT'; layout: 'one-per-page' | 'two-per-page' }
  | { type: 'SET_STYLE'; style: 'decorated' | 'plain' }
  | { type: 'TOGGLE_AUDIO'; hymnId: string; field: string }
  | { type: 'SELECT_ALL_AUDIO'; selected: boolean }
  | { type: 'SET_GENERATING'; isGenerating: boolean }
  | { type: 'SET_ERROR'; error: string | null }
  | { type: 'RESET' };

export function useWizardReducer(): [WizardState, React.Dispatch<WizardAction>];
```

From app/empaquetador/hooks/useHymnSearch.ts:
```typescript
export function useHymnSearch(): {
  query: string;
  setQuery: (q: string) => void;
  hymnal: string;
  setHymnal: (h: string) => void;
  category: string;
  setCategory: (c: string) => void;
  results: HymnSearchResult[];
  isLoading: boolean;
  error: string | null;
};
```

From app/interfaces/Hymn.interface.ts:
```typescript
export interface HymnSearchResult {
  id: string; name: string; hymn_number: number | null;
  hymnal: { id: string; name: string } | null;
  categories: Array<{ hymn_categories_id: { id: number; name: string } | null }>;
  audioFiles: HymnAudioFiles; hasAnyAudio: boolean;
}
```

shadcn imports from '@/lib/shadcn/ui':
Button, Input, Badge, Card, ScrollArea, Separator,
Select, SelectContent, SelectItem, SelectTrigger, SelectValue,
Drawer, DrawerContent, DrawerTrigger
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WizardStepper component and EmpaquetadorPage shell</name>
  <files>
    app/empaquetador/components/WizardStepper.tsx,
    app/empaquetador/page.tsx
  </files>
  <read_first>
    app/empaquetador/hooks/useWizardReducer.ts,
    .planning/phases/04-wizard-ui-and-download-experience/04-UI-SPEC.md (Sections 6, 7, 9, 11, 12),
    .planning/phases/04-wizard-ui-and-download-experience/04-RESEARCH.md (WizardStepper code example),
    app/page.tsx (existing page pattern reference)
  </read_first>
  <action>
    **1. Create WizardStepper component** (`app/empaquetador/components/WizardStepper.tsx`):
    - `'use client'` directive not needed (receives props, no hooks)
    - Import `Check` from `lucide-react`, `cn` from `@/app/lib/shadcn/utils`
    - Define `STEPS` array: `[{ number: 1, label: 'Seleccionar Himnos' }, { number: 2, label: 'Configurar' }, { number: 3, label: 'Descargar' }]`
    - Props: `{ currentStep: 1|2|3, onStepClick: (step: 1|2|3) => void }`
    - Render horizontal nav with `role="navigation"` and `aria-label="Pasos del empaquetador"` (per UI-SPEC Section 12)
    - Use `flex items-center justify-center gap-4` layout
    - Between circles: connector line `div` with `h-0.5 flex-1`, `bg-primary` if completed, `bg-border` if not
    - Circle: `w-10 h-10 rounded-full` on desktop, `w-8 h-8` on mobile (use responsive: `w-8 h-8 sm:w-10 sm:h-10`)
    - States per UI-SPEC Section 7:
      - Completed (step < currentStep): `bg-primary text-primary-foreground` + Check icon (h-4 w-4)
      - Active (step === currentStep): `bg-primary text-primary-foreground` + step number
      - Upcoming (step > currentStep): `bg-muted text-muted-foreground` + step number
    - Labels: `text-sm hidden sm:block`, bold for active/completed, muted for upcoming
    - Completed circles: `cursor-pointer`, onClick calls `onStepClick(step.number)`
    - Upcoming: `cursor-default`, disabled, no onClick
    - ARIA: `aria-current="step"` on active, `aria-label="Paso N completado"` on completed
    - Export default

    **2. Create EmpaquetadorPage** (`app/empaquetador/page.tsx`):
    - `'use client'` directive (per D-01)
    - Import `useWizardReducer` from hooks, `WizardStepper` from components
    - Import `AnimatePresence, motion` from `framer-motion` (per UI-SPEC Section 11)
    - Import `useReducedMotion` from `framer-motion` for accessibility (per UI-SPEC Section 12)
    - Call `const [state, dispatch] = useWizardReducer()`
    - Track animation direction with `useRef<'forward' | 'backward'>('forward')` — update on step changes
    - Page layout: `max-w-4xl mx-auto px-4 py-8` (per UI-SPEC Section 6)
    - Render `<h1 className="text-2xl font-semibold leading-tight mb-6">Empaquetador de Himnos</h1>` (per UI-SPEC Section 9)
    - Render `<WizardStepper currentStep={state.step} onStepClick={(s) => { dirRef.current = 'backward'; dispatch({ type: 'SET_STEP', step: s }); }} />`
    - Use AnimatePresence with `mode="wait"`:
      - If reduced motion: no animation wrapper, just render step component
      - Otherwise: `motion.div` with key={state.step}, slide transitions per UI-SPEC Section 11:
        - Forward: initial x:20/opacity:0, animate x:0/opacity:1, exit x:-20/opacity:0, duration 0.2
        - Backward: initial x:-20/opacity:0, animate x:0/opacity:1, exit x:20/opacity:0, duration 0.2
    - Conditional rendering: step 1 -> `<StepSeleccion>`, step 2 -> placeholder `<div>Paso 2: Configurar</div>`, step 3 -> placeholder `<div>Paso 3: Descargar</div>`
    - Bottom navigation bar: flex justify-between, mt-6
      - "Atras" button (variant="outline"): hidden on step 1, onClick dispatches SET_STEP to step-1
      - "Siguiente" button (variant="default"): hidden on step 3, disabled when step 1 and selectedHymns.length === 0 (per D-18)
      - onClick for Siguiente: set direction to 'forward', dispatch SET_STEP to step+1
    - Pass to StepSeleccion: `state`, `dispatch` as props
  </action>
  <verify>
    <automated>npx vitest run tests/empaquetador/ --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - app/empaquetador/page.tsx contains `'use client'`
    - app/empaquetador/page.tsx contains `useWizardReducer`
    - app/empaquetador/page.tsx contains `AnimatePresence`
    - app/empaquetador/page.tsx contains `Empaquetador de Himnos`
    - app/empaquetador/page.tsx contains `Siguiente`
    - app/empaquetador/page.tsx contains `max-w-4xl`
    - app/empaquetador/components/WizardStepper.tsx contains `Seleccionar Himnos`
    - app/empaquetador/components/WizardStepper.tsx contains `Configurar`
    - app/empaquetador/components/WizardStepper.tsx contains `Descargar`
    - app/empaquetador/components/WizardStepper.tsx contains `aria-label="Pasos del empaquetador"`
    - app/empaquetador/components/WizardStepper.tsx contains `Check` icon import from lucide-react
    - `npx vitest run tests/empaquetador/ --reporter=verbose` exits 0 (pure-function tests still pass)
  </acceptance_criteria>
  <done>WizardStepper renders 3 steps with correct visual states. EmpaquetadorPage has working step navigation with Framer Motion transitions. Siguiente is disabled when no hymns selected. Completed steps are clickable for backward navigation.</done>
</task>

<task type="auto">
  <name>Task 2: StepSeleccion with search, filters, results, and selected hymns panel</name>
  <files>
    app/empaquetador/components/StepSeleccion.tsx,
    app/empaquetador/components/HymnResultRow.tsx,
    app/empaquetador/components/SelectedHymnChip.tsx
  </files>
  <read_first>
    app/empaquetador/hooks/useHymnSearch.ts,
    app/empaquetador/hooks/useWizardReducer.ts,
    app/empaquetador/page.tsx,
    .planning/phases/04-wizard-ui-and-download-experience/04-UI-SPEC.md (Sections 6, 8, 9, 10, 13),
    .planning/phases/04-wizard-ui-and-download-experience/04-RESEARCH.md (Pattern 4 mobile drawer)
  </read_first>
  <action>
    **1. Create HymnResultRow component** (`app/empaquetador/components/HymnResultRow.tsx`):
    - Props: `{ hymn: HymnSearchResult, isSelected: boolean, onAdd: (hymn: HymnSearchResult) => void }`
    - Render as a Card with `p-3 flex items-center justify-between min-h-[44px]` (per UI-SPEC touch target)
    - Left side: hymn_number (bold, `text-sm font-semibold w-12`) + name (`text-sm truncate flex-1`) + hymnal Badge (`variant="secondary" text-xs`) if hymnal exists
    - Right side: if isSelected, render `<Badge variant="outline">Seleccionado</Badge>` (per D-08, UI-SPEC Section 9). If not selected, render `<Button variant="default" size="sm" onClick={() => onAdd(hymn)}>Agregar</Button>`
    - When selected: add `bg-muted/50` to card background (per UI-SPEC Section 8)
    - Export default

    **2. Create SelectedHymnChip component** (`app/empaquetador/components/SelectedHymnChip.tsx`):
    - Props: `{ hymn: HymnSearchResult, onRemove: (hymnId: string) => void }`
    - Render as flex row: hymn_number + name (truncated) + X button
    - X button: `<Button variant="ghost" size="icon" onClick={() => onRemove(hymn.id)} aria-label="Quitar">` with Lucide `X` icon (16px) (per UI-SPEC Section 9)
    - `min-h-[44px]` for touch target
    - Export default

    **3. Create StepSeleccion component** (`app/empaquetador/components/StepSeleccion.tsx`):
    - `'use client'` directive (uses hooks)
    - Props: `{ state: WizardState, dispatch: React.Dispatch<WizardAction> }`
    - Call `useHymnSearch()` for search state
    - Fetch hymnal and category lists on mount with `useEffect`:
      - `fetch('/api/hymnals')` -> parse JSON -> store in `useState<Array<{id:string, name:string}>>([])` as `hymnals`
      - `fetch('/api/categories')` -> parse JSON -> store in `useState<Array<{id:number, name:string}>>([])` as `categories`
      - While loading, dropdowns show "Cargando..." placeholder and are `disabled` (per UI-SPEC Section 10)
    - Step heading: `<h2 className="text-xl font-semibold">Seleccionar Himnos</h2>` (per UI-SPEC Section 9)
    - Filter row (per D-05): two shadcn Select components side by side on sm+, stacked on mobile
      - Hymnal: `<Select value={hymnal} onValueChange={setHymnal}>` with default item "Todos los himnarios" (value="")
      - Category: `<Select value={category} onValueChange={setCategory}>` with default item "Todas las categorias" (value="")
    - Search input (per D-04): `<Input placeholder="Buscar por numero o nombre..." value={query} onChange={e => setQuery(e.target.value)} aria-label="Buscar himnos" />`
    - Results area:
      - Loading: 3 skeleton divs with `animate-pulse bg-muted rounded-md h-14` (per UI-SPEC Section 10)
      - Error: `<p className="text-destructive text-sm">Error al buscar. Intenta de nuevo.</p>` (per UI-SPEC Section 9)
      - Empty + no query: `<p className="text-muted-foreground text-sm">Escribe el numero o nombre de un himno para comenzar.</p>`
      - Empty + has query: `<p className="text-muted-foreground text-sm">No se encontraron himnos. Intenta con otro termino de busqueda.</p>`
      - Results: `<ScrollArea className="h-[400px] lg:h-[400px] h-[300px]">` containing HymnResultRow for each result
      - Check if hymn is selected: `state.selectedHymns.some(h => h.id === hymn.id)`
      - On add: `dispatch({ type: 'ADD_HYMN', hymn })`

    - Desktop sidebar (per D-07): `<div className="hidden lg:block w-80">` containing:
      - Title: "Himnos Seleccionados" + Badge with count
      - ScrollArea with SelectedHymnChip for each selected hymn
      - Empty state: "Aun no has seleccionado himnos. Busca y agrega himnos desde los resultados."
      - On remove: `dispatch({ type: 'REMOVE_HYMN', hymnId })`

    - Mobile drawer (per D-07, Research Pattern 4, UI-SPEC Section 13):
      - `<div className="lg:hidden">` wrapper
      - Only visible when `state.selectedHymns.length > 0`
      - Trigger: fixed bottom bar `fixed bottom-0 left-0 right-0 h-14 bg-card border-t shadow-lg z-40`
      - Trigger text: "Seleccionados" + `<Badge>{count}</Badge>`
      - DrawerContent: same SelectedHymnChip list as sidebar
      - Add `pb-16` to step content on mobile when hymns selected (per Research Pitfall 5) to prevent overlap with nav buttons

    - Layout: on lg+, use `flex gap-6` with results column `flex-1` and sidebar `w-80` (per UI-SPEC Section 6)
  </action>
  <verify>
    <automated>npx vitest run tests/empaquetador/ --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - app/empaquetador/components/StepSeleccion.tsx contains `'use client'`
    - app/empaquetador/components/StepSeleccion.tsx contains `useHymnSearch`
    - app/empaquetador/components/StepSeleccion.tsx contains `fetch('/api/hymnals')`
    - app/empaquetador/components/StepSeleccion.tsx contains `fetch('/api/categories')`
    - app/empaquetador/components/StepSeleccion.tsx contains `Buscar por numero o nombre`
    - app/empaquetador/components/StepSeleccion.tsx contains `Todos los himnarios`
    - app/empaquetador/components/StepSeleccion.tsx contains `Todas las categorias`
    - app/empaquetador/components/StepSeleccion.tsx contains `Himnos Seleccionados`
    - app/empaquetador/components/StepSeleccion.tsx contains `Drawer`
    - app/empaquetador/components/HymnResultRow.tsx contains `Agregar`
    - app/empaquetador/components/HymnResultRow.tsx contains `Seleccionado`
    - app/empaquetador/components/SelectedHymnChip.tsx contains `Quitar` or `aria-label="Quitar"`
    - `npx vitest run tests/empaquetador/ --reporter=verbose` exits 0 (pure-function tests still pass)
  </acceptance_criteria>
  <done>Step 1 is fully functional: user can search hymns with debounce, filter by hymnal/category, add hymns via Agregar button, see selected hymns in sidebar (desktop) or drawer (mobile), and remove hymns. Empty states and loading skeletons render correctly. All UI text in Spanish.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/empaquetador/ --reporter=verbose` — pure-function tests still pass
- `npm run build` — no type errors
- Navigate to /empaquetador — wizard stepper renders, search works, add/remove hymns works
</verification>

<success_criteria>
- /empaquetador page renders with wizard stepper showing 3 steps
- Search-as-you-type returns results from /api/hymns/search with 300ms debounce
- Hymnal and category filter dropdowns populate from API and filter results
- Agregar button adds hymn to selection, row shows Seleccionado state
- Selected hymns visible in desktop sidebar and mobile drawer
- X button removes hymn from selection
- Siguiente disabled when no hymns selected, enabled with 1+ hymns
- Atras button hidden on step 1
- Step transitions animate with Framer Motion slide
- All text in Spanish per UI-SPEC Section 9
</success_criteria>

<output>
After completion, create `.planning/phases/04-wizard-ui-and-download-experience/04-02-SUMMARY.md`
</output>
