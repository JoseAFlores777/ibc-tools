---
phase: 04-wizard-ui-and-download-experience
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - app/empaquetador/components/StepConfiguracion.tsx
  - app/empaquetador/components/AudioTrackRow.tsx
  - app/empaquetador/components/StepDescarga.tsx
  - app/empaquetador/page.tsx
  - app/page.tsx
autonomous: false
requirements:
  - AUDIO-01
  - AUDIO-02
  - AUDIO-03
  - UX-01
  - UX-02
  - UX-03

must_haves:
  truths:
    - "User can choose between 1-per-page and 2-per-page layout via radio buttons"
    - "User can choose between decorated and plain style via radio buttons"
    - "User sees expandable accordion per hymn showing only available audio tracks"
    - "User can toggle individual audio tracks with checkboxes"
    - "Seleccionar todas las pistas checkbox toggles all available tracks"
    - "Hymns with no audio show Sin pistas disponibles label"
    - "User can click Generar Paquete and see progress indicator"
    - "ZIP file auto-downloads on success with success toast"
    - "Error shows toast with retry button"
    - "Crear otro paquete resets wizard to step 1"
    - "Landing page has a link to /empaquetador"
  artifacts:
    - path: "app/empaquetador/components/StepConfiguracion.tsx"
      provides: "Step 2: print layout/style radio groups + audio accordion"
      contains: "Impresion"
    - path: "app/empaquetador/components/AudioTrackRow.tsx"
      provides: "Single audio track checkbox within accordion"
      contains: "Checkbox"
    - path: "app/empaquetador/components/StepDescarga.tsx"
      provides: "Step 3: summary card, generate button, progress, download"
      contains: "Generar Paquete"
    - path: "app/page.tsx"
      provides: "Landing page with Empaquetador link"
      contains: "empaquetador"
  key_links:
    - from: "app/empaquetador/components/StepDescarga.tsx"
      to: "/api/hymns/package"
      via: "fetch POST with PackageRequest body"
      pattern: "fetch.*api/hymns/package"
    - from: "app/empaquetador/components/StepDescarga.tsx"
      to: "app/empaquetador/lib/build-package-request.ts"
      via: "import buildPackageRequest"
      pattern: "buildPackageRequest"
    - from: "app/empaquetador/page.tsx"
      to: "StepConfiguracion and StepDescarga"
      via: "conditional rendering in step switch"
      pattern: "StepConfiguracion|StepDescarga"
---

<objective>
Complete the wizard with Step 2 (print/audio configuration), Step 3 (generate and download), wire them into the page, and add the Empaquetador link to the landing page.

Purpose: This completes the entire user flow from hymn selection through configuration to package download. All Phase 4 requirements are satisfied after this plan.
Output: Fully functional 3-step wizard at /empaquetador. Landing page links to it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wizard-ui-and-download-experience/04-CONTEXT.md
@.planning/phases/04-wizard-ui-and-download-experience/04-UI-SPEC.md
@.planning/phases/04-wizard-ui-and-download-experience/04-01-SUMMARY.md
@.planning/phases/04-wizard-ui-and-download-experience/04-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 and Plan 02 outputs -->

From app/empaquetador/hooks/useWizardReducer.ts:
```typescript
export interface WizardState {
  step: 1 | 2 | 3;
  selectedHymns: HymnSearchResult[];
  layout: 'one-per-page' | 'two-per-page';
  style: 'decorated' | 'plain';
  audioSelections: Map<string, Set<string>>;
  isGenerating: boolean;
  error: string | null;
}
export type WizardAction = /* 10 action types including TOGGLE_AUDIO, SELECT_ALL_AUDIO, SET_GENERATING, SET_ERROR, RESET */;
```

From app/empaquetador/lib/build-package-request.ts:
```typescript
export function buildPackageRequest(state: WizardState): PackageRequest;
```

From app/lib/zip/zip.schema.ts:
```typescript
export type PackageRequest = {
  hymns: { id: string; audioFiles: string[] }[];
  layout: 'one-per-page' | 'two-per-page';
  style: 'decorated' | 'plain';
};
```

From app/interfaces/Hymn.interface.ts:
```typescript
export interface HymnAudioFiles {
  track_only: AudioFileInfo | null;
  midi_file: AudioFileInfo | null;
  soprano_voice: AudioFileInfo | null;
  alto_voice: AudioFileInfo | null;
  tenor_voice: AudioFileInfo | null;
  bass_voice: AudioFileInfo | null;
}
```

Audio field to Spanish label map (UI-SPEC Section 9):
track_only -> "Pista completa", midi_file -> "MIDI",
soprano_voice -> "Soprano", alto_voice -> "Alto",
tenor_voice -> "Tenor", bass_voice -> "Bajo"

shadcn imports from '@/lib/shadcn/ui':
RadioGroup, RadioGroupItem, Accordion, AccordionContent, AccordionItem, AccordionTrigger,
Checkbox, Label, Progress, Button, Card, Separator, Badge
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StepConfiguracion with print config and audio accordion</name>
  <files>
    app/empaquetador/components/StepConfiguracion.tsx,
    app/empaquetador/components/AudioTrackRow.tsx
  </files>
  <read_first>
    app/empaquetador/hooks/useWizardReducer.ts,
    app/empaquetador/page.tsx,
    .planning/phases/04-wizard-ui-and-download-experience/04-UI-SPEC.md (Sections 6, 8, 9),
    .planning/phases/04-wizard-ui-and-download-experience/04-RESEARCH.md (Select All Audio code example)
  </read_first>
  <action>
    **1. Create AudioTrackRow component** (`app/empaquetador/components/AudioTrackRow.tsx`):
    - Props: `{ field: string, label: string, checked: boolean, onToggle: (field: string) => void }`
    - Render `<div className="flex items-center gap-2 min-h-[44px]">` (per UI-SPEC touch target)
    - Checkbox: `<Checkbox id={field} checked={checked} onCheckedChange={() => onToggle(field)} />`
    - Label: `<Label htmlFor={field} className="text-sm cursor-pointer">{label}</Label>`
    - Export default

    **2. Create StepConfiguracion component** (`app/empaquetador/components/StepConfiguracion.tsx`):
    - Props: `{ state: WizardState, dispatch: React.Dispatch<WizardAction> }`
    - No `'use client'` needed (receives props, no hooks)

    - **Print section** (per D-09, UI-SPEC Section 6 Step 2 layout):
      - Section title: `<h2 className="text-xl font-semibold">Configurar Impresion y Audio</h2>`
      - Card wrapper with `p-4`
      - Subsection: `<h3 className="text-base font-semibold mb-3">Impresion</h3>`
      - Layout RadioGroup: `value={state.layout}`, `onValueChange={(v) => dispatch({ type: 'SET_LAYOUT', layout: v as 'one-per-page' | 'two-per-page' })}`, orientation vertical
        - RadioGroupItem value="one-per-page" label "1 himno por pagina"
        - RadioGroupItem value="two-per-page" label "2 himnos por pagina"
      - Separator between layout and style
      - Style RadioGroup: `value={state.style}`, `onValueChange={(v) => dispatch({ type: 'SET_STYLE', style: v as 'decorated' | 'plain' })}`, orientation vertical
        - RadioGroupItem value="decorated" label "Decorado"
        - RadioGroupItem value="plain" label "Plano"

    - **Audio section** (per D-10, D-11, D-12):
      - Subsection: `<h3 className="text-base font-semibold mb-3">Audio</h3>`
      - Define constant AUDIO_LABELS map: `{ track_only: 'Pista completa', midi_file: 'MIDI', soprano_voice: 'Soprano', alto_voice: 'Alto', tenor_voice: 'Tenor', bass_voice: 'Bajo' }` (per UI-SPEC Section 9)
      - "Seleccionar todas las pistas" checkbox (per D-11):
        - Compute `allSelected`: check if every hymn with audio has all its available tracks selected
        - `<Checkbox checked={allSelected} onCheckedChange={(checked) => dispatch({ type: 'SELECT_ALL_AUDIO', selected: Boolean(checked) })} />`
        - Label: "Seleccionar todas las pistas"
      - Accordion: `<Accordion type="multiple" className="w-full">`
        - For each hymn in `state.selectedHymns`:
          - If `!hymn.hasAnyAudio`: render non-interactive row with `<p className="text-muted-foreground text-sm py-3">Sin pistas disponibles</p>` (per D-12)
          - If `hymn.hasAnyAudio`: render AccordionItem with:
            - Trigger: `Himno {hymn_number || '?'} - {hymn.name}`
            - Content: for each field in AUDIO_LABELS, if `hymn.audioFiles[field] !== null`, render AudioTrackRow
            - checked: `state.audioSelections.get(hymn.id)?.has(field) ?? false`
            - onToggle: `dispatch({ type: 'TOGGLE_AUDIO', hymnId: hymn.id, field })`
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/empaquetador/components/StepConfiguracion.tsx contains `Configurar Impresion y Audio`
    - app/empaquetador/components/StepConfiguracion.tsx contains `RadioGroup`
    - app/empaquetador/components/StepConfiguracion.tsx contains `1 himno por pagina`
    - app/empaquetador/components/StepConfiguracion.tsx contains `2 himnos por pagina`
    - app/empaquetador/components/StepConfiguracion.tsx contains `Decorado`
    - app/empaquetador/components/StepConfiguracion.tsx contains `Plano`
    - app/empaquetador/components/StepConfiguracion.tsx contains `Seleccionar todas las pistas`
    - app/empaquetador/components/StepConfiguracion.tsx contains `Accordion`
    - app/empaquetador/components/StepConfiguracion.tsx contains `Sin pistas disponibles`
    - app/empaquetador/components/StepConfiguracion.tsx contains `TOGGLE_AUDIO`
    - app/empaquetador/components/AudioTrackRow.tsx contains `Checkbox`
    - app/empaquetador/components/AudioTrackRow.tsx contains `Label`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Step 2 renders print layout/style radio groups with correct defaults. Audio accordion shows per-hymn tracks with checkboxes. Select-all toggle works. Hymns without audio show muted label. All labels in Spanish.</done>
</task>

<task type="auto">
  <name>Task 2: StepDescarga, wire steps into page, and add landing page link</name>
  <files>
    app/empaquetador/components/StepDescarga.tsx,
    app/empaquetador/page.tsx,
    app/page.tsx
  </files>
  <read_first>
    app/empaquetador/page.tsx,
    app/empaquetador/lib/build-package-request.ts,
    app/empaquetador/hooks/useWizardReducer.ts,
    app/page.tsx,
    .planning/phases/04-wizard-ui-and-download-experience/04-UI-SPEC.md (Sections 8, 9, 14),
    .planning/phases/04-wizard-ui-and-download-experience/04-RESEARCH.md (Pattern 3 blob download, Pitfall 4 object URL, Pitfall 6 progress)
  </read_first>
  <action>
    **1. Create StepDescarga component** (`app/empaquetador/components/StepDescarga.tsx`):
    - Props: `{ state: WizardState, dispatch: React.Dispatch<WizardAction> }`
    - Import `buildPackageRequest` from `../lib/build-package-request`
    - Import `toast` from `sonner`
    - Import `Progress, Button, Card, Separator, Badge` from `@/lib/shadcn/ui`

    - **Summary card** (per D-13, UI-SPEC Section 14):
      - Step heading: `<h2 className="text-xl font-semibold">Generar y Descargar</h2>`
      - Card with `p-4 rounded-lg`:
        - `{state.selectedHymns.length} himno(s)` with Separator
        - Layout: `state.layout === 'one-per-page' ? '1 por pagina' : '2 por pagina'` with Separator
        - Style: `state.style === 'decorated' ? 'Decorado' : 'Plano'` with Separator
        - Audio count: compute total selected tracks = sum of all Set sizes in audioSelections Map. Show `{total} pista(s) de audio` or `Sin pistas de audio` if 0

    - **Generate button and progress** (per D-13, D-14, D-15):
      - `<Button size="lg" disabled={state.isGenerating} onClick={handleGenerate} className="w-full sm:w-auto">`
      - Button text: `state.isGenerating ? 'Generando...' : 'Generar Paquete'` (per UI-SPEC Section 9)
      - When `state.isGenerating`: show `<Progress value={100} className="animate-pulse" />` below button (per Research Pitfall 6 — pass value={100} + animate-pulse for indeterminate)

    - **handleGenerate async function** (per D-14, Research Pattern 3):
      - `dispatch({ type: 'SET_GENERATING', isGenerating: true })`
      - `dispatch({ type: 'SET_ERROR', error: null })`
      - try:
        - `const body = buildPackageRequest(state)`
        - `const res = await fetch('/api/hymns/package', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) })`
        - If `!res.ok`: throw new Error('Server error')
        - `const blob = await res.blob()`
        - Create object URL, create `<a>` element, set href/download='himnos.zip', click, remove, revoke URL (per Research Pitfall 4)
        - `toast.success('Paquete descargado exitosamente!')` (per D-14, but without the upside-down exclamation per actual toast pattern)
        - Set `downloadComplete` local state to true
      - catch:
        - `dispatch({ type: 'SET_ERROR', error: 'Error al generar el paquete.' })`
        - `toast.error('Error al generar el paquete. Intenta de nuevo.', { action: { label: 'Reintentar', onClick: () => handleGenerate() } })` (per D-15)
      - finally:
        - `dispatch({ type: 'SET_GENERATING', isGenerating: false })`

    - **Post-download** (per D-16):
      - When downloadComplete is true: show `<Button variant="outline" onClick={() => dispatch({ type: 'RESET' })}>Crear otro paquete</Button>`

    **2. Update EmpaquetadorPage** (`app/empaquetador/page.tsx`):
    - Add imports for `StepConfiguracion` and `StepDescarga`
    - Replace step 2 placeholder with `<StepConfiguracion state={state} dispatch={dispatch} />`
    - Replace step 3 placeholder with `<StepDescarga state={state} dispatch={dispatch} />`
    - On step 3, hide the "Siguiente" button (already should be per existing logic, but verify)
    - On step 3, hide the "Atras" button OR keep it (per D-18 "Atras" is available on all steps except step 1). Keep Atras visible on step 3.

    **3. Add Empaquetador link to landing page** (per D-03):
    - Read `app/page.tsx` first
    - Add a new Button link in the button list (after "Horarios" and before "Facebook"):
      ```tsx
      <Button size={'lg'} variant="outline" asChild>
        <Link href={'/empaquetador'}>Empaquetador de Himnos</Link>
      </Button>
      ```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/empaquetador/components/StepDescarga.tsx contains `Generar y Descargar`
    - app/empaquetador/components/StepDescarga.tsx contains `Generar Paquete`
    - app/empaquetador/components/StepDescarga.tsx contains `Generando...`
    - app/empaquetador/components/StepDescarga.tsx contains `Crear otro paquete`
    - app/empaquetador/components/StepDescarga.tsx contains `buildPackageRequest`
    - app/empaquetador/components/StepDescarga.tsx contains `fetch('/api/hymns/package'`
    - app/empaquetador/components/StepDescarga.tsx contains `URL.createObjectURL`
    - app/empaquetador/components/StepDescarga.tsx contains `URL.revokeObjectURL`
    - app/empaquetador/components/StepDescarga.tsx contains `toast.success`
    - app/empaquetador/components/StepDescarga.tsx contains `toast.error`
    - app/empaquetador/components/StepDescarga.tsx contains `Reintentar`
    - app/empaquetador/components/StepDescarga.tsx contains `animate-pulse`
    - app/empaquetador/page.tsx contains `StepConfiguracion`
    - app/empaquetador/page.tsx contains `StepDescarga`
    - app/page.tsx contains `empaquetador`
    - app/page.tsx contains `Empaquetador de Himnos`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Step 2 and Step 3 fully wired into wizard. Complete user flow works: search hymns -> configure print/audio -> generate ZIP -> download. Landing page links to /empaquetador. All UI text in Spanish.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of complete wizard</name>
  <files>app/empaquetador/page.tsx</files>
  <read_first>app/empaquetador/page.tsx</read_first>
  <action>
    Run `npm run dev` and open http://localhost:3000 in the browser. Walk through the complete wizard flow to verify all 3 steps work correctly. This is a human verification checkpoint — no code changes expected unless issues are found.
  </action>
  <what-built>
    Complete 3-step Empaquetador de Himnos wizard at /empaquetador:
    - Step 1: Search hymns with debounce, filter by hymnal/category, add/remove hymns
    - Step 2: Print layout/style radio buttons, audio track accordion with checkboxes
    - Step 3: Package summary, generate with progress, auto-download ZIP
    - Landing page link at /
    - Framer Motion step transitions
    - Mobile responsive with drawer for selected hymns
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Verify "Empaquetador de Himnos" link appears on landing page
    3. Click it to navigate to /empaquetador
    4. Verify 3-step stepper shows at top with "Seleccionar Himnos" active
    5. Type a hymn number in search box, verify results appear after ~300ms
    6. Type a hymn name, verify name search works
    7. Test hymnal and category filter dropdowns
    8. Click "Agregar" on a result — verify it appears in sidebar (desktop) or drawer trigger shows count (mobile)
    9. Click X to remove a hymn, verify it disappears
    10. Add at least 2 hymns, click "Siguiente"
    11. Verify Step 2: radio buttons for layout and style, audio accordion per hymn
    12. Toggle audio checkboxes, test "Seleccionar todas las pistas"
    13. Click "Siguiente" to Step 3
    14. Verify summary card shows correct counts
    15. Click "Generar Paquete" — verify progress indicator, then ZIP downloads
    16. Verify success toast appears
    17. Click "Crear otro paquete" — verify wizard resets to Step 1
    18. Test "Atras" button navigation between steps
    19. Test clicking completed step circles to jump back
    20. Resize browser to mobile width — verify drawer instead of sidebar
  </how-to-verify>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms all 20 verification steps pass
    - No visual regressions or broken interactions reported
  </acceptance_criteria>
  <done>Human has verified the complete wizard flow works end-to-end: search, select, configure, generate, download.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` — no type errors
- `npx vitest run --exclude '.claude/**' --reporter=verbose` — all tests pass
- Complete manual walkthrough of wizard flow
</verification>

<success_criteria>
- Step 2 shows layout/style radio groups with correct defaults (1-per-page, decorated)
- Audio accordion expands per hymn, shows only available tracks as checkboxes
- Select-all checkbox toggles all available tracks
- Hymns with no audio show "Sin pistas disponibles"
- Step 3 summary card shows correct hymn count, layout, style, audio track count
- Generar Paquete button triggers POST, shows progress, auto-downloads ZIP
- Success and error toasts work (Sonner)
- Crear otro paquete resets entire wizard
- Landing page has Empaquetador de Himnos link
- All UI text in Spanish
- Human verifies complete flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/04-wizard-ui-and-download-experience/04-03-SUMMARY.md`
</output>
