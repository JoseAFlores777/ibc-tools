---
phase: quick
plan: 260329-ve4
type: execute
wave: 1
depends_on: []
files_modified:
  - app/sections/Navbar.tsx
  - app/herramientas/page.tsx
  - app/page.tsx
  - app/empaquetador/page.tsx
  - app/empaquetador/components/StepSeleccion.tsx
  - app/empaquetador/components/HymnResultRow.tsx
  - app/empaquetador/components/StepDescarga.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "Navbar shows Logo IBC, Inicio, Herramientas, Horarios links and a working hamburger menu on mobile"
    - "/herramientas page renders a card grid with Empaquetador de Himnos linking to /empaquetador"
    - "Home page groups internal tools separately from external social links"
    - "Empaquetador fixes: accent on Atras, search above filters, clickable Seleccionado badge, honest loading indicator, hymn names in summary"
  artifacts:
    - path: "app/sections/Navbar.tsx"
      provides: "Rewritten navbar with real navigation"
    - path: "app/herramientas/page.tsx"
      provides: "Herramientas launcher page"
    - path: "app/page.tsx"
      provides: "Updated home page layout"
    - path: "app/empaquetador/page.tsx"
      provides: "Fixed accent and navigation header"
    - path: "app/empaquetador/components/StepSeleccion.tsx"
      provides: "Search input above filters, Button for drawer trigger"
    - path: "app/empaquetador/components/HymnResultRow.tsx"
      provides: "Clickable Seleccionado badge that deselects"
    - path: "app/empaquetador/components/StepDescarga.tsx"
      provides: "Honest loading spinner, hymn name list in summary"
  key_links:
    - from: "app/sections/Navbar.tsx"
      to: "/herramientas"
      via: "Next.js Link"
      pattern: "Link.*href.*herramientas"
    - from: "app/herramientas/page.tsx"
      to: "/empaquetador"
      via: "Next.js Link"
      pattern: "Link.*href.*empaquetador"
---

<objective>
UX/UI audit fixes: rewrite the placeholder navbar with real navigation, create the /herramientas launcher page, update the home page layout, and fix multiple empaquetador UX issues.

Purpose: The app currently has a demo navbar, no tools landing page, and several small UX bugs in the empaquetador wizard. This plan brings the navigation and polish up to production quality.
Output: 7 files modified across navbar, new page, home page, and empaquetador components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/sections/Navbar.tsx
@app/page.tsx
@app/layout.tsx
@app/empaquetador/page.tsx
@app/empaquetador/components/StepSeleccion.tsx
@app/empaquetador/components/HymnResultRow.tsx
@app/empaquetador/components/StepDescarga.tsx

<interfaces>
From app/lib/shadcn/ui (barrel exports available):
- Button, Badge, Card, CardHeader, CardTitle, CardDescription, CardContent
- Input, ScrollArea, Select, SelectContent, SelectItem, SelectTrigger, SelectValue
- Drawer, DrawerContent, DrawerTrigger, DrawerTitle
- Separator, Progress, Skeleton
- Sheet, SheetContent, SheetTrigger (available for mobile menu)
- NavigationMenu components (available but optional)

From app/empaquetador/hooks/useWizardReducer.ts:
```typescript
export type WizardAction =
  | { type: 'REMOVE_HYMN'; hymnId: string }
  // ... other actions
```

From app/empaquetador/components/HymnResultRow.tsx:
```typescript
interface HymnResultRowProps {
  hymn: HymnSearchResult;
  isSelected: boolean;
  onAdd: (hymn: HymnSearchResult) => void;
}
```

Icons available: lucide-react (already in project), @radix-ui/react-icons (HamburgerMenuIcon, Cross1Icon)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Navbar, create /herramientas page, update home page</name>
  <files>app/sections/Navbar.tsx, app/herramientas/page.tsx, app/page.tsx</files>
  <action>
**app/sections/Navbar.tsx** -- Complete rewrite. Remove ALL accordion/placeholder content (Nav component, getNavLinks, AccordionItems with "Is it accessible?" etc.). Keep the 'use client' directive and the glass-morphism backdrop-blur styling pattern. New structure:

- Fixed top bar with: IBC logo (Image, link to "/"), nav links on desktop (Inicio "/", Herramientas "/herramientas", Horarios "/horarios") as text links with hover states, hamburger button on mobile (use HamburgerMenuIcon/Cross1Icon from @radix-ui/react-icons which are already imported).
- Mobile menu: AnimatePresence + motion.div slide-down (keep existing animation pattern). Inside: vertical stack of Link items (same 3 routes). Each link closes the menu on click (setIsOpen(false)).
- Keep the existing useEffect that toggles body overflow.
- Active link highlighting: compare pathname (already available via usePathname) and apply a visual indicator (font-bold or underline).
- Clean minimal design: `bg-white/10 backdrop-blur-lg` on scroll/open (existing pattern). Logo size ~50px (smaller than current 80px for a nav bar).
- Remove unused imports: Accordion*, ScrollArea, Separator, Nav, all lucide icons (AlertCircle, Archive, etc.), File, Inbox, MessagesSquare, Search, Send, ShoppingCart, Trash2, Users2.

**app/herramientas/page.tsx** -- New file. Server component (no 'use client' needed).

- Page title: `<h1>` "Herramientas"
- Subtitle: brief description like "Aplicaciones internas de IBC"
- Grid layout: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6`
- One card for now: **Empaquetador de Himnos**
  - Use Card/CardHeader/CardTitle/CardDescription from shadcn
  - Large icon: Package icon from lucide-react
  - Title: "Empaquetador de Himnos"
  - Description: "Selecciona himnos, configura letras y audio, y descarga un ZIP listo para usar."
  - Entire card is a Link to "/empaquetador" with hover:shadow-lg transition
- Import Navbar and render it at the top of the page (consistent with home page pattern)
- Mobile responsive via the grid classes

**app/page.tsx** -- Update the home page layout:

- Keep the hero image, Navbar, and welcome heading
- Replace the flat `<div className="flex flex-col mt-5 gap-5 w-10/12">` button list with two grouped sections:
  1. **Herramientas** section: a sub-heading "Herramientas" followed by buttons for Herramientas (link to /herramientas), Horarios (link to /horarios). These are internal app links.
  2. **Redes Sociales** section: a sub-heading "Redes Sociales" followed by existing external links (Facebook, Youtube, Radio IBC, Descarga nuestra App). Keep target='_blank' on external links.
- Remove the direct "Empaquetador de Himnos" button (users reach it via /herramientas now)
- Update the intro text: remove "Haz click en uno de estos enlaces:" -- replace with something cleaner or remove entirely
- Keep the existing absolute positioning and gradient overlay structure
  </action>
  <verify>
    <automated>cd /Volumes/Samsung_SSD_990_EVO_Plus_4TB_Media/Documents/Church/Projects/Alabanza/ibc-tools && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Navbar shows real navigation with 3 links and hamburger menu. /herramientas page renders with Empaquetador card. Home page has grouped sections for tools vs social links. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Fix empaquetador UX issues</name>
  <files>app/empaquetador/page.tsx, app/empaquetador/components/StepSeleccion.tsx, app/empaquetador/components/HymnResultRow.tsx, app/empaquetador/components/StepDescarga.tsx</files>
  <action>
**app/empaquetador/page.tsx:**
- Line 90: Fix "Atras" to "Atras" with accent: use the proper unicode character. The string should be rendered with proper Spanish accent marks. Change `Atras` to the correctly accented form with tilde on the a.
- Add arrow icons to navigation buttons: import ChevronLeft and ChevronRight from lucide-react. Back button: `<ChevronLeft className="w-4 h-4 mr-1" />` before text. Next button: text then `<ChevronRight className="w-4 h-4 ml-1" />` after text.
- Add a navigation header at the top of the page (above the h1): a Link to /herramientas with a ChevronLeft icon and text "Herramientas" styled as a subtle back-link (`text-sm text-muted-foreground hover:text-foreground flex items-center gap-1 mb-4`).

**app/empaquetador/components/StepSeleccion.tsx:**
- Move the search Input (currently at lines 134-140, after the filter grid) to ABOVE the filter dropdowns. Cut the `<Input placeholder="Buscar por numero o nombre..." ...>` block and paste it before the `<div className="grid grid-cols-1 sm:grid-cols-2 gap-3 mb-3">` filter grid. Adjust margin: Input gets `mb-3`, filter grid keeps `mb-3` (change filter grid's `mb-3` to `mb-4` for spacing before results).
- Line 187-191: Replace the raw `<button type="button" className="fixed bottom-0...">` with a shadcn `<Button variant="outline" className="fixed bottom-0 left-0 right-0 h-14 z-40 flex items-center justify-center gap-2 rounded-none border-x-0 border-b-0">`. This ensures consistent styling with the rest of the app.

**app/empaquetador/components/HymnResultRow.tsx:**
- Make the "Seleccionado" Badge clickable to deselect. The component needs a new prop: `onRemove: (hymnId: string) => void`.
- Update the interface: add `onRemove` to HymnResultRowProps.
- Replace the static `<Badge variant="outline">Seleccionado</Badge>` with a clickable badge: wrap it in a `<button>` or change it to `<Badge variant="outline" className="cursor-pointer hover:bg-destructive/10 hover:text-destructive transition-colors" onClick={() => onRemove(hymn.id)}>Seleccionado x</Badge>`. Add a small "x" or use the X icon from lucide-react inside the badge.
- Update the caller in StepSeleccion.tsx: where `<HymnResultRow>` is rendered (around line 166), add `onRemove={(hymnId) => dispatch({ type: 'REMOVE_HYMN', hymnId })}` prop.

**app/empaquetador/components/StepDescarga.tsx:**
- Line 101: Replace `<Progress value={100} className="animate-pulse" />` with an honest indeterminate loading indicator. Use a `<div>` with a Tailwind animation: a bar that slides back and forth. Example: a container div with `overflow-hidden rounded-full h-2 bg-muted` containing an inner div with `h-full w-1/3 bg-primary rounded-full animate-[indeterminate_1.5s_ease-in-out_infinite]`. Add the keyframe to the component via a `<style>` tag or use a simpler approach: `<Progress value={undefined}` if supported, OR just use `<div className="h-2 bg-muted rounded-full overflow-hidden"><div className="h-full w-1/3 bg-primary rounded-full animate-bounce" /></div>`. The key requirement: do NOT show a full 100% bar that pulses -- show movement that communicates "working" honestly.
- In the summary Card (lines 70-88): after the `<p>{state.selectedHymns.length} himno(s)</p>` line, add a collapsible or always-visible list of selected hymn names. Add: `<ul className="mt-1 space-y-0.5 text-muted-foreground">{state.selectedHymns.map(h => <li key={h.id}>{h.hymn_number ? `#${h.hymn_number} - ` : ''}{h.name}</li>)}</ul>` before the Separator. This shows users exactly which hymns are in the package.
  </action>
  <verify>
    <automated>cd /Volumes/Samsung_SSD_990_EVO_Plus_4TB_Media/Documents/Church/Projects/Alabanza/ibc-tools && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Empaquetador shows: accented "Atras" with arrow icons, back-link to /herramientas, search input above filters, shadcn Button for drawer trigger, clickable "Seleccionado" badge that deselects, indeterminate progress indicator, and hymn names listed in summary card. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete UX/UI audit: rewritten navbar with real navigation, new /herramientas launcher page, updated home page with grouped sections, and all empaquetador fixes (accent, search order, clickable badge, honest progress, hymn names in summary).</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000
    2. Verify navbar shows IBC logo + Inicio/Herramientas/Horarios links on desktop
    3. Resize to mobile width -- verify hamburger menu works, links close menu on tap
    4. Click "Herramientas" in navbar -- verify /herramientas page shows card grid with Empaquetador card
    5. Click the Empaquetador card -- verify it navigates to /empaquetador
    6. On /empaquetador: verify "Herramientas" back-link at top, arrow icons on nav buttons, accented "Atras"
    7. Verify search input is ABOVE the filter dropdowns
    8. Search for a hymn, add it, verify "Seleccionado" badge is clickable and removes the hymn
    9. On mobile: verify the bottom drawer trigger is a proper styled Button
    10. Advance to step 3 (Descarga): verify selected hymn names appear in summary card
    11. Click "Generar Paquete": verify progress indicator is an indeterminate animation (not a full pulsing bar)
    12. Go back to home page: verify tools section is separated from social media links
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- All 7 files modified compile correctly
- No broken imports (removed unused imports from Navbar)
- /herramientas route resolves (new page.tsx exists)
</verification>

<success_criteria>
- Navbar is functional with real navigation (not placeholder accordion)
- /herramientas page exists with Empaquetador tool card
- Home page has clear separation between internal tools and external links
- All 5 empaquetador UX issues fixed
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260329-ve4-ux-ui-audit-rewrite-navbar-create-herram/260329-ve4-SUMMARY.md`
</output>
