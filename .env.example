# Variables de Entorno - Ejemplo
# Copia este archivo a `.env` y ajusta los valores según tu entorno.
# Nota: Las variables que comienzan con NEXT_PUBLIC_ serán expuestas al navegador.
# Formato requerido: KEY=value (sin comillas). Las líneas con # son comentarios.

# --- Aplicación ---
# URL base del servidor Directus usada por el backend de Next.js.
DIRECTUS_URL=http://localhost:8055

# URL pública de Directus para el navegador. Si no se define, se usa DIRECTUS_URL.
NEXT_PUBLIC_DIRECTUS_URL=http://localhost:8055

# --- Chatwoot (Widget en el navegador) ---
# URL base del servidor de Chatwoot que sirve el SDK (expone al navegador)
NEXT_PUBLIC_CHATWOOT_BASE_URL=https://atencion.ibchn.org
# Token del sitio de Chatwoot (expone al navegador)
NEXT_PUBLIC_CHATWOOT_WEBSITE_TOKEN=Mzd5zuHL9p4EsjdK3Na7dYqQ

# --- CI/CD (Jenkins) ---
# En Jenkins, crea una credencial de tipo "Secret file" con este archivo .env.
# El pipeline cargará este archivo para obtener las credenciales y parámetros necesarios.
# Namespace/usuario de Docker Hub (por ejemplo: myorg o myuser)
DOCKERHUB_NAMESPACE=
# Nombre del repositorio en Docker Hub (por ejemplo: ibc-tools)
DOCKERHUB_REPOSITORY=ibc-tools
# Credenciales de Docker Hub
DOCKERHUB_USERNAME=
DOCKERHUB_TOKEN=
# Registro de Docker (opcional). Por defecto: docker.io
DOCKER_REGISTRY=docker.io
